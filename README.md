## reactel-to-html-server

```
curl -X POST "https://html-to-jsx-to-html-server-git-feature-newapi.ojisan.vercel.app/api/" \
  -H "Content-Type: application/json" \
  -d '{
  "html": "<div className={`taro`} style={{color: `red`, display: `flex`}}></div>"
}'
```
